const week = [
  'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday',
  'Saturday', 'Sunday'
];

week.sort();

const fruits = ['watermelon', 'apple', 'orange', 'kiwi'];

fruits.sort((a, b) => a.length - b.length);

console.log(week);
console.log(fruits);
