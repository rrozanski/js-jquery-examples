let color;
let favouriteColor = 'Blue';

// const today; // Error, const value must be initialized.
const today = 'Monday';

console.log(favouriteColor, today);

if (color === undefined) {
  let favouriteColor = 'Red';
  const today = 'Friday';
  console.log(favouriteColor, today);
}
